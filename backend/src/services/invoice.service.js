const PDFDocument = require("pdfkit"); const fs = require("fs"); exports.generateInvoice = async ( booking ) => { const doc = new PDFDocument(); const path = `uploads/invoice-${booking.id}.pdf`; doc.pipe( fs.createWriteStream(path) ); doc.fontSize(26) .text("AM38 Invoice"); doc.moveDown(); doc.text( `Booking: ${booking.id}` ); doc.text( `Customer: ${booking.customer_name}` ); doc.text( `Amount: Rs ${booking.total_amount}` ); doc.end(); return path; };